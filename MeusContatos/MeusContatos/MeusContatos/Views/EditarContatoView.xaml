<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:MeusContatos.Views"
             x:Class="MeusContatos.Views.EditarContatoView"
             Title="Editar/Deletar Contatos">
    <ContentPage.Content>
        <StackLayout Padding="10">
            <local:CadastroContatoView />

            <Button Text="Editar Contato" 
                    TextColor="White"
                    BackgroundColor="#1E90FF" 
                    Command="{Binding EditarContatoCommand}"/>

            <Button Text="Deletar Contato" 
                    TextColor="White"
                    BackgroundColor="#1E90FF" 
                    Command="{Binding DeletarContatoCommand}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>